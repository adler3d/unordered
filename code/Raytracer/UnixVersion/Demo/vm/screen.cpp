
out.size=vec2i(256,256);

out.map
pix is t_pixel





vector<t_point> update(const vector<t_point>&arr){
  auto arr0=do_grav(arr);
  auto arr1=do_move(arr0);
  return arr;
}

vector<vector<t_point>> t;

for(;@;){
  qap_add_back(t)=update(t.back());
}

пройтись по всем пиксел€м.
посчитать сколько точек попало внутрь.
вернуть это как цвет.

функци€ котора€ вычисл€ет координаты границ пикселей.

тоесть граница пикселей будет задана батаном. батан - это така€ функци€ котора€ вычисл€ет константу...

f(screen.center.pos.x,screen.w)...

auto q=get_quad(campos,wh,x,y)

n=0;
points.map
  ex
  if(abs((q.pos.x-ex.pos.x)<<1)<wh.x)n++;

return n;

std::array<t_vertex,4> VA=q.get_VA();

t_point p0=VA[0];

string x=p0.x;

string dx=abs(points[0].pos.x-x);


1-0+0 != 1-0


